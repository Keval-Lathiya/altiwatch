#pragma once
// Stub for ESP32 Arduino core 2.x — periman was added in core 3.x.
// Arduino_GFX includes this header for the SPI bus driver; since we use
// the RGB panel driver only, the SPI bus is never instantiated at runtime.
