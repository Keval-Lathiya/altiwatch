#pragma once
// Stub: esp_private/periph_ctrl.h is not present in Arduino ESP32 2.x / IDF 4.x.
// Arduino_GFX includes it for the SPI bus driver; we only use the RGB panel so
// none of the SPI bus code is instantiated at runtime.
